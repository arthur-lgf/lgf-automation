# Intentionally empty — the Slack listener (api/slack.py + app/services/slack_commands.py)
# uses only the Python standard library (hmac, hashlib, json, urllib). Keeping this
# empty makes the Vercel function tiny and cold-start fast (matters for Slack's 3s ack).
